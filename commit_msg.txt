feat: AI security, PWA offline support, optimistic UI & calendar tests

- Implement rate limiting for AI endpoints (finance, reset, kitchen).
- Update Service Worker for robust caching strategy (Cache-First for Next.js static assets) enabling offline PWA functionality.
- Apply Optimistic UI updates to event task toggling and kitchen shopping cart actions, ensuring immediate feedback.
- Add Jest unit tests for calendar planner logic (`sortByDate`, `buildMonthGrid`, `isSameDay`).